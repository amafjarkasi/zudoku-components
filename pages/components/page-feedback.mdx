---
title: Page Feedback
description: Was this page helpful? feedback component
---

# Feedback

Let us know if this documentation was helpful.

<div class="feedback-container">
  <div class="feedback-question">Was this page helpful?</div>
  <div class="feedback-buttons">
    <button class="feedback-btn feedback-btn--yes" data-feedback="yes">
      <span class="feedback-icon">👍</span>
      <span>Yes</span>
    </button>
    <button class="feedback-btn feedback-btn--no" data-feedback="no">
      <span class="feedback-icon">👎</span>
      <span>No</span>
    </button>
  </div>
  <div class="feedback-response feedback-response--yes" style={{display: "none"}}>
    <div class="feedback-message">Thanks for the feedback! Glad we could help.</div>
  </div>
  <div class="feedback-response feedback-response--no" style={{display: "none"}}>
    <div class="feedback-message">
      <strong>Sorry to hear that.</strong>
      <span>How can we improve this page?</span>
      <textarea class="feedback-textarea" placeholder="Tell us what's missing or unclear..."></textarea>
      <button class="feedback-submit">Submit Feedback</button>
    </div>
  </div>
</div>

## Other Ways to Reach Us

<div class="support-links">
  <a href="https://discord.gg/zu" class="support-link">
    <span class="support-icon">💬</span>
    <div class="support-content">
      <strong>Join our Discord</strong>
      <span>Chat with developers and get help</span>
    </div>
  </a>
  <a href="https://github.com/zusdk/sdk/issues" class="support-link">
    <span class="support-icon">🐛</span>
    <div class="support-content">
      <strong>Report an Issue</strong>
      <span>Found a bug? Let us know on GitHub</span>
    </div>
  </a>
  <a href="/guides/error-handling" class="support-link">
    <span class="support-icon">📖</span>
    <div class="support-content">
      <strong>Troubleshooting Guide</strong>
      <span>Common issues and solutions</span>
    </div>
  </a>
</div>



<script>{`
  document.addEventListener('DOMContentLoaded', function() {
    const yesBtn = document.querySelector('.feedback-btn--yes');
    const noBtn = document.querySelector('.feedback-btn--no');
    const responseYes = document.querySelector('.feedback-response--yes');
    const responseNo = document.querySelector('.feedback-response--no');
    const submitBtn = document.querySelector('.feedback-submit');

    yesBtn.addEventListener('click', function() {
      yesBtn.classList.add('selected');
      noBtn.classList.remove('selected');
      responseYes.style.display = 'block';
      responseNo.style.display = 'none';
    });

    noBtn.addEventListener('click', function() {
      noBtn.classList.add('selected');
      yesBtn.classList.remove('selected');
      responseNo.style.display = 'block';
      responseYes.style.display = 'none';
    });

    if (submitBtn) {
      submitBtn.addEventListener('click', function() {
        const textarea = document.querySelector('.feedback-textarea');
        submitBtn.textContent = 'Sent!';
        submitBtn.disabled = true;
        textarea.disabled = true;
      });
    }
  });
`}</script>